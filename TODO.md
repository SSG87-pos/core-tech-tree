# TODO

- 실제 Core 기술 약 100개 규모의 XLSX를 업로드해 렌더링 성능, 트리 가독성, 필터 사용성을 확인한다.
- 실제 조직명 데이터를 넣은 뒤 `조직 목록 편집` 기준 목록과 XLSX 내 조직명이 잘 맞는지 검토한다.
- 연구그룹/Team 외에 TF/PJ/임시조직을 별도 타입으로 통계 분리해야 하는지 결정한다.
- 조직 관점 화면에서 담당자 수만 볼지, 담당자 이름 목록까지 펼쳐볼지 결정한다.
- 업로드 데이터를 명시적으로 삭제하고 데모 데이터로 복원하는 버튼이 필요한지 결정한다.
- Level 1/Level 2 변경은 현재 드래그 이동 중심이다. 상세 팝오버에서도 직접 입력 UI가 필요한지 결정한다.
- 기술 추가/삭제/드래그 이동 후 JSON/XLSX 내보내기 파일을 실제 업무 파일로 재업로드하는 왕복 테스트를 진행한다.
- 최종 배포가 필요해지면 HTML과 `data/core-tech-data.json`을 같은 폴더 루트로 배포한다.
- 사내 표준 색상/폰트가 확정되면 CSS 변수만 조정한다.
- Excel 템플릿 컬럼이 바뀌면 `docs/DATA_SCHEMA.md`, HTML 내 XLSX import/export, Python converter를 같이 수정한다.
- JSON schema를 변경할 때는 `schemaVersion`, `orgMaster`, `rows` 호환성을 함께 확인한다.
